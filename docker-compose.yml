version: "3.9"

services:
  graph-render:
    build: .
    ports:
      - "${GRAPHRENDER_PORT:-8080}:8080"
    environment:
      - GRAPHRENDER_PORT=8080
      - GRAPHRENDER_ICON_CACHE_DIR=/app/.cache/icons
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')",
        ]
      interval: 10s
      timeout: 5s
      start_period: 5s
      retries: 3
